# Setup Instructions

- Python version: 3.8+
- Install ACR1252U driver from ACS website.
- Run `python -m venv venv` to create a virtual environment.
- Activate: `source venv/Scripts/activate` (Windows).
- Install dependencies: `pip install -r docs/requirements.txt`.
